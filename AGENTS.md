## Structure

This repo is the shared web app source for Nuvio TV app. Main code is in `js/`, styles in `css/`, assets in `assets/`, and build scripts in `scripts/`. Output goes to `dist/`.

Wrapper repos:
- webOS: ``
- Tizen: ``



